import React from 'react';

 export const metadata = {
   title: "Add Task : Work Manager",
 };

const page = () => {
    return (
        <div>
            <h1>Add Task Component</h1>
            
        </div>
    );
};

export default page;