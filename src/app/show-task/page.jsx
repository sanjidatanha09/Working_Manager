import React from 'react';

 export const metadata = {
   title: "Show Task : Work Manager",
 };

const page = () => {
    return (
        <div>
            <h1>Show task page</h1>
        </div>
    );
};

export default page;